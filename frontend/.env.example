# Frontend build-time environment variables
# Dev: leave blank — Vite proxy handles /api/* → http://localhost:8000
# Prod: set to the absolute URL of your deployed backend, e.g.
#   VITE_API_BASE_URL=https://nouri-scan-api.onrender.com
VITE_API_BASE_URL=
